# Production Dependencies for Streamlit Cloud Deployment
# This is a subset of requirements.txt - only what's needed to run the dashboard

# Core
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0

# Dashboard
streamlit>=1.28.0
plotly>=5.18.0

# ML Inference (not training)
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# Risk Metrics
scipy>=1.11.0

# HTTP Requests (for remote model loading)
requests>=2.31.0

# Data types
pydantic>=2.5.0

# Logging
loguru>=0.7.0

# Note: The following are excluded from deployment requirements:
# - transformers, torch (sentiment model - too large for cloud)
# - arch (GARCH models - optional)
# - vectorbt (backtesting - not needed for predictions)
# - fastapi, uvicorn (API server - dashboard only)
# - pytest, pytest-cov (testing)
# - yfinance (data collection - done locally)
# - nltk (sentiment - done locally)
